import { ConvertToFeetPipe } from './convert-to-feet.pipe';

describe('ConvertToFeetPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToFeetPipe();
    expect(pipe).toBeTruthy();
  });
});
